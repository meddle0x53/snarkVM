# program_process_load_from_store

This test measures the amount of time it takes to load a VM from DB.
To run the test:
```
# Mainnet
cargo run --release -- mainnet
# Testnet
cargo run --release -- testnet
```
